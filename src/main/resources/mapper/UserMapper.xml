<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.helpdesk.user.mapper.UserMapper">

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId"
            parameterType="com.helpdesk.user.domain.User">
        INSERT INTO users (username, password, name, email, role)
        VALUES (#{username}, #{password}, #{name}, #{email}, #{role})
    </insert>

    <select id="findByUsername" resultType="com.helpdesk.user.domain.User">
        SELECT user_id, username, password, name, email, role, created_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*) FROM users WHERE username = #{username}
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>

</mapper>
